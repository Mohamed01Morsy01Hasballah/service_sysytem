class AddonsModel{
  String? icon;
  String? title;
  String? subtitle;
  String? price;
  bool? sale;
  String? previousPrice;

  AddonsModel(this.icon, this.title, this.subtitle, this.price,
      this.previousPrice, this.sale);

}